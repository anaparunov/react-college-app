{
  "name": "reactapp",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "browserify -t [ babelify --presets [ react es2015 ] ] main.js -o build.js -v",
    "watch": "watchify -d -e -t [ babelify --presets [ react es2015 ] ] main.js -o build.js -v",
    "dev:serve": "live-server --port=9090 ./",
    "dev": "npm run build && parallelshell \"npm run watch\" \"npm run dev:serve\""
  },
  "author": "ana@parunov.com",
  "license": "ISC",
  "devDependencies": {
    "browserify": "^13.1.0",
    "babelify": "^7.3.0",
    "babel-preset-es2015": "^6.14.0",
    "babel-preset-react": "^6.11.1",
    "watchify": "^3.7.0",
    "live-server": "^1.1.0"
  },
  "dependencies": {
    "parallelshell": "^2.0.0",
    "react": "^0.14.3",
    "react-dom": "^0.14.3"
  }
}
